abstract class Routes {
  Routes._();
  static const HOME = _Paths.HOME;
  static const EVENTS = _Paths.EVENTS; // Tambahkan ini
}

abstract class _Paths {
  _Paths._();
  static const HOME = '/home';
  static const EVENTS = '/events'; // Tambahkan ini
}